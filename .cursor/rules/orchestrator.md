# Cursor orchestrator rules

- Prefer shared modules from `helpers/` and `pages/`.
- Keep Playwright config aligned with `tests/`.
- Preserve compatibility with the existing vulnerable-bank test suite.
