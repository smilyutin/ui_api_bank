meta {
  name: Approve loan
  type: http
  seq: 4
  tags: [
    admin
  ]
}

post {
  url: {{baseUrl}}/admin/approve_loan/:loan_id
  body: none
  auth: bearer
}

params:path {
  loan_id: 
}

auth:bearer {
  token: {{token}}
}

docs {
  Approve user loan request.
}

example {
  name: 200 Response
  description: Loan approved
  
  request: {
    url: {{baseUrl}}/admin/approve_loan/:loan_id
    method: POST
    mode: none
    params:path: {
      loan_id: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
