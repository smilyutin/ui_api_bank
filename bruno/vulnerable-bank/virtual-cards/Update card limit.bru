meta {
  name: Update card limit
  type: http
  seq: 5
  tags: [
    virtual-cards
  ]
}

post {
  url: {{baseUrl}}/api/virtual-cards/:card_id/update-limit
  body: json
  auth: bearer
}

params:path {
  card_id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "limit": 0
  }
}

docs {
  Update the spending limit of a virtual card. Vulnerable to BOLA.
}

example {
  name: 200 Response
  description: Card limit updated successfully
  
  request: {
    url: {{baseUrl}}/api/virtual-cards/:card_id/update-limit
    method: POST
    mode: json
    params:path: {
      card_id: 
    }
  
    body:json: {
      {
        "limit": 0
      }
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
