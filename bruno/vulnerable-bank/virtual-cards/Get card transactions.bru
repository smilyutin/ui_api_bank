meta {
  name: Get card transactions
  type: http
  seq: 4
  tags: [
    virtual-cards
  ]
}

get {
  url: {{baseUrl}}/api/virtual-cards/:card_id/transactions
  body: none
  auth: bearer
}

params:path {
  card_id: 
}

auth:bearer {
  token: {{token}}
}

docs {
  Retrieve transaction history for a virtual card. Vulnerable to BOLA and SQL injection.
}

example {
  name: 200 Response
  description: Transaction history retrieved successfully
  
  request: {
    url: {{baseUrl}}/api/virtual-cards/:card_id/transactions
    method: GET
    mode: none
    params:path: {
      card_id: 
    }
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
